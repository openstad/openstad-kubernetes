---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "openstad.frontend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openstad.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ template "openstad.frontend.fullname" . }}-deployment
spec:
  replicas: {{ .Values.frontend.replicas }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: {{ .Values.frontend.label }}
  template:
    metadata:
      labels:
        app: {{ .Values.frontend.label }}
    spec:
      imagePullSecrets:
        - name: regcred2
      containers:
        - name: {{ template "openstad.frontend.fullname" . }}
          image: {{ .Values.frontend.deploymentContainer.image }}
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: {{ .Values.frontend.service.httpPort }}
              protocol: TCP
          env:
            - name: DEFAULT_HOST
              value: www.{{ .Values.host.base }}
            - name: APP_URL
              value: https://www.{{ .Values.host.base }}
            - name: API
              value: https://{{ template "openstad.api.url" . }}
            - name: OLD_IMAGE_API_URL
              value: https://{{ template "openstad.image.url" . }}
            - name: IMAGE_API_URL
              value: "https://image-server2.openstadsdeel.nl"
            - name: MINIFY_JS
              value: 'ON'
            - name: APOS_WORKFLOW
              value: 'ON'
            - name: GOOGLE_MAPS_API_KEY
              value: "{{ .Values.frontend.googleMapsApiKey }}"
            - name: COOKIE_MAX_AGE
              value: "{{ .Values.frontend.cookieMaxAge }}"
            - name: VIDEO_API_KEY
              value: "79287de14026563c6be87240ff58f170b025f"
            - name: VIDEO_API_EMAIL
              value: "tosh.koevoets@ymove.app"
            - name: VIDEO_API_URL
              value: "https://api.cloudflare.com/client/v4/accounts/09157f80735764e831c779e509cb1252/stream"
            - name: IMAGE_API_ACCESS_TOKEN
              value: "VkajDDVm0KnDaajsjA23AdS"
            - name: OLD_IMAGE_API_ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  key: image_api_token
                  name: openstad-auth-credentials

            - name: HOST_DOMAIN
              valueFrom:
                secretKeyRef:
                  key: frontend
                  name: openstad-hosts
            - name: PORT
              value: {{ .Values.frontend.service.httpPort | quote }}
            - name: SITE_API_KEY
              valueFrom:
                secretKeyRef:
                  key: fixed_token
                  name: openstad-auth-credentials

            - name: MONGO_DB_HOST
              valueFrom:
                secretKeyRef:
                  key: hostname
                  name: openstad-mongo-credentials
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  key: hostname
                  name: openstad-mongo-credentials
            - name: DEFAULT_DB
              valueFrom:
                secretKeyRef:
                  key: database
                  name: openstad-mongo-credentials
            - name: KUBERNETES_NAMESPACE
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.namespace
            - name: MY_POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP




          resources:
{{ toYaml .Values.frontend.resources | indent 12 }}
          volumeMounts:
            - mountPath: /home/app/public/uploads
              name: data-uploads
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsUser: 1000
      volumes:
        - name: data-uploads
          persistentVolumeClaim:
            claimName: frontend-uploads-claim2
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - {{ .Values.frontend.label }}
            topologyKey: "kubernetes.io/hostname"
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 50
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - {{ .Values.image.label }}
              topologyKey: doks.digitalocean.com/node-id
