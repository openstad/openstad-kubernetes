---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.database.auth.secretName }}
  namespace: {{ .Release.namespace }}
  annotations:
    "helm.sh/resource-policy": keep
data:
  {{ template "authDbSecret" . }}