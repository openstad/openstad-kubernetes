---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.cookies.secretName }}
  namespace: {{ .Release.namespace }}
  annotations:
    "helm.sh/resource-policy": keep
data:
  {{ template "cookieSecret" . }}