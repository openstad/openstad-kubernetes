---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.database.auth.credentials.secretName }}
  namespace: {{ .Values.secrets.namespace }}
  annotations:
    "helm.sh/resource-policy": keep
data:
  {{ template "authCredSecret" . }}